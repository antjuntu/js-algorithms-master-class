let names = ['Michael', 'Melissa', 'Andrea']
console.log(names)

// push pop

// shift unshift

// concat

// slice

// splice

// sort

// forEach map filter reduce etc.

let first = names.shift()
console.log('first', first)
console.log(names)

let length = names.unshift('Paavo')
console.log('length', length)
console.log(names)

console.log('pop:')
const popped = names.pop()
console.log(popped, names)